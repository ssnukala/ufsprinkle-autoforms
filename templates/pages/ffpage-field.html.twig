{%if field.type=='select' %}
    {{  ff_widget.af_select(field)}}
{% elseif field.type=='checkbox' %}
    {{  ff_widget.af_select(field)}}
{% elseif field.type=='textarea' %}
    {{  ff_widget.af_textarea(field)}}
{% else %}
    {{ ff_widget.af_inputtext(field) }}
{% endif %}
